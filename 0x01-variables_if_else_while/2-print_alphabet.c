#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (success)
 */

/* more headers goes there */
int main(void)
{
	int cha;

	for (cha = 'a'; cha <= 'z'; cha++)
		putchar(cha);

	putchar('\n');

	return (0);
}
